" Habilita a numeração das linhas
set number

" Destaca a busca enquanto digita
set incsearch

" Ignora maiúsculas e minúsculas nas buscas
set ignorecase

" Destaque as buscas
set hlsearch

" Usa tabulação de 4 espaços
set tabstop=4
set shiftwidth=4
set expandtab

" Habilita o autocompletamento
set completeopt=menuone,noinsert,noselect

" Mostra a linha de status
set laststatus=2

" Ativa a indentação automática
filetype plugin indent on

" Habilita o modo de linha de comando incremental
set wildmenu

" Destaca a linha atual
set cursorline

" Ativa suporte a cor
syntax enable

" Color theme
colorscheme elflord

